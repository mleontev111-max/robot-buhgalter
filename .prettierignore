dist
node_modules
src/components/ui
checkpoints

# Формат этих файлов сознательно не унифицирован Prettier —
# правки здесь не были частью изначальной проблемы (плотный однострочный код в src/**).
*.md
tailwind.config.js
tsconfig*.json
vite.config.ts

# Канонические machine-readable state-файлы Resume Gate v1.3 — их формат
# принадлежит project_ready-треку и читается tools/*.py; переформатировать
# их отсюда нельзя, иначе пойдут лишние конфликты с main.
ACTIVE_WORK.json
PROJECT_READY.json
PROJECT_STATE.json
CHECKPOINT_INDEX.json
MILESTONE_INDEX.json
