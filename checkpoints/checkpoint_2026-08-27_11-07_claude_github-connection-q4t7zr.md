# 🚀 ЧЕКПОИНТ: Robot-Buhgalter

## 📌 Мета-информация
- **Дата:** 2026-08-27 11:07 (UTC+3)
- **Автор:** Claude Sonnet 5 (сессия `claude/github-connection-q4t7zr`)
- **Ветка:** `claude/github-connection-q4t7zr` (1:1 с `main` до коммитов этой сессии)
- **Commit:** `ec563d7`
- **Этап:** стабилизация MVP — технический аудит закрывается пункт за пунктом

Это независимая сессия поверх работы «GPT-5.6 Sol + Михаил» (чекпоинты
`checkpoint_2026-08-25_*`). Веду тот же список рисков, ничего не дублирую.

---

## 🗺️ Где мы находимся

### ✅ Что сделано в этой сессии (после 25.08)
1. **Доступ к GitHub восстановлен.** Push из сессии сначала падал с 403
   (GitHub App не была авторизована на аккаунте); после переустановки
   `Install & Authorize` в connectors всё заработало — коммиты пушатся
   напрямую в `claude/github-connection-q4t7zr` → далее в `main` без PR
   (ветка сейчас = `main` + наши коммиты, PR не создавался, т.к. явно не просили).
2. **Prettier** (`ac64fa5`): `.prettierrc.json`/`.prettierignore`, отформатирован
   весь `src/**` (кроме `components/ui/**`, это shadcn-генерация) и `server/**`.
   Плотные однострочные файлы (`Connections.tsx`, `Dashboard.tsx`, `tax.ts`,
   `marketplaces.mjs` и т.д.) теперь читаемы. `npm run format` / `format:check`,
   `format:check` добавлен в оба GitHub Actions workflow (`ci.yml`, `deploy-pages.yml`).
   Доки/конфиги (`README.md`, `tailwind.config.js`, `tsconfig*.json`, `vite.config.ts`)
   сознательно не тронуты — не были частью исходной проблемы, шум в диффе.
3. **Налоговые правила по годам** (`ec563d7`): `TAX_2026` (захардкоженный объект)
   заменён на реестр `TAX_RULES_BY_YEAR: Record<year, TaxYearRules>` +
   `resolveTaxRules(year)`. `TAX_2026` оставлен алиасом на `TAX_RULES_BY_YEAR[2026]`
   для обратной совместимости — ничего снаружи не сломано.
   - `calcTax`, `calcUsnVat`, `calcInsuranceForYear`, прогрессивный НДФЛ ОСНО —
     все теперь принимают опциональный `year` и резолвят правила через реестр.
   - Если для года правил ещё нет (например, наступит 2027-й, а ставки ФНС
     ещё не занесены) — **не тихая ошибка**: берутся правила последнего
     известного года + в `notes` явно добавляется предупреждение с указанием
     обоих годов. Никаких цифр на 2027 год не выдумано — только сама
     инфраструктура фоллбека.
   - Заодно починен реальный баг: `quarterlyAdvances(store, ops, year)` уже
     принимал `year` (в UI есть выбор года), но не прокидывал его в `calcTax`,
     поэтому расчёт квартальных авансов всегда шёл по 2026 году независимо
     от выбранного. Теперь прокидывается.
   - `calcOrganizationTax` берёт год из `from` периода, добавлено поле
     `OrganizationTaxSummary.notes` для того же предупреждения на уровне ИП.
   - Тесты: `tax.test.ts` и `organizationTax.test.ts` дополнены (14 → 19),
     старые assertion'ы не менялись — поведение для 2026 года побитово то же.

### Проверено перед каждым коммитом
`tsc -b --noEmit`, `eslint .`, `prettier --check .`, `vitest run`, `vite build` —
всё зелёное на каждом шаге. Формально CI на GitHub Actions **не запускался**
для этих двух коммитов (workflow триггерится на `pull_request` или `push` в
`main`, а не на push в фиче-ветку без PR) — все проверки прогнаны только
локально в сессии.

### 🔄 В работе прямо сейчас
Ничего не в процессе — оба изменения закоммичены, запушены, проверены.
Ветка `claude/github-connection-q4t7zr` = `main` + 2 коммита сверху.
PR не открыт (не просили explicit).

---

## 🎯 Ближайшие шаги (актуальный бэклог из аудита)

| Приоритет | Задача | Статус |
|---|---|---|
| P0 | Данные и credentials вынести из browser localStorage в backend | Не начато |
| P0 | API-ключи маркетплейсов — серверное secret storage вместо localStorage | Не начато |
| P1 | Production sync backend вместо `localhost:8787` (публичный сайт + localhost — dev-only схема) | Не начато |
| P1 | Тесты на `quarterlyAdvances` и `taxCalendar.ts` (сейчас регрессия только по `calcTax`/`calcOrganizationTax`) | Следующий тестовый блок |
| P2 | Открыть PR из `claude/github-connection-q4t7zr` в `main`, если пользователь захочет формальный ревью | По запросу |
| P2 | Удалить мёртвые ветки `feature/tax-2026`, `architecture/multi-tenant-domain-model` (уже смёржены в main 19.08, просто не удалены) | Заблокировано разрешениями сессии на удаление веток — нужно вручную или явное разрешение |
| P2 | Code-splitting: `vite build` предупреждает про чанк 825 KB | Не начато |

---

## 🧠 Контекст и архитектурные решения

- Реестр `TAX_RULES_BY_YEAR` — это **инфраструктура**, не данные на будущее.
  Когда появятся официальные параметры 2027 года — добавить запись в реестр,
  весь остальной код (`calcTax`, `calcOrganizationTax`, `quarterlyAdvances`)
  подхватит её без изменений сигнатур.
- `TAX_2026` и `calcInsurance2026` оставлены как публичные экспорты специально —
  на них всё ещё завязан внешний код (`organizationTax.ts` раньше, теперь
  переведён на реестр; но паттерн сохранён для любого нового кода, который
  предпочтёт явно зафиксироваться на 2026 годе).
- Ветки `feature/tax-2026` / `architecture/multi-tenant-domain-model` на GitHub —
  не чья-то параллельная работа, а мусор от уже смёрженных 19.08 PR #1/#2.
  GitHub не удаляет ветки автоматически после мёржа.
- Работа над проектом ведётся минимум двумя независимыми сессиями/агентами
  (эта + «GPT-5.6 Sol»), чекпоинты — общий канал синхронизации между ними.
  Перед началом любой сессии — сверяться с `git log`, `checkpoints/*.md` и
  открытыми ветками/PR на GitHub, а не только с локальным контекстом.

### Ключевые файлы
```bash
src/lib/tax.ts
src/lib/organizationTax.ts
src/lib/tax.test.ts
src/lib/organizationTax.test.ts
src/lib/taxCalendar.ts
src/lib/storage.ts
src/sections/Connections.tsx
server/index.mjs
.prettierrc.json / .prettierignore
.github/workflows/ci.yml
.github/workflows/deploy-pages.yml
```

### Незакоммиченные изменения
```bash
Нет незакоммиченных изменений
```

---

## ⚠️ Риски и блокеры

| Проблема | Уровень | Действие |
|---|---|---|
| Бухгалтерские данные и credentials только в localStorage | 🔴 Высокий | PostgreSQL/backend — не начато |
| API-ключи маркетплейсов хранятся в браузере в открытом виде | 🔴 Высокий | Backend secret storage — не начато |
| CI не запускался на реальном GitHub Actions для последних 2 коммитов (нет PR) | 🟡 Средний | Открыть PR или дождаться мёржа в `main`, чтобы получить реальный (не только локальный) прогон |
| `localhost:8787` sync-сервер vs. публичный HTTPS-сайт (Private Network Access) | 🟠 Важный | Production sync API — не начато |
| Bundle 825 KB в одном чанке | 🟡 Средний | code-splitting — не начато |
| Мёртвые ветки на GitHub засоряют список | 🟢 Низкий | Удалить вручную или разрешить сессии `git push --delete` |

---

## 💬 Последнее обсуждение
> **Пользователь:** «давай создавать промежуточные чекпоинты, чтобы не терять нить проекта, фиксировать результаты, видеть общий план» и «отлично продолжай» (после Prettier).
>
> **Результат:** Prettier закоммичен и запушен; следом — рефакторинг налоговых
> правил по годам, тоже закоммичен и запушен; этот файл — первый чекпоинт
> от этой сессии, дальше буду фиксировать так же после каждого содержательного шага.

---

## 🔄 Как восстановить работу
1. `git log --oneline -10` — проверить, что `ec563d7` всё ещё голова ветки
   (или посмотреть, что смёржено дальше).
2. Свериться с `checkpoints/checkpoint_2026-08-25_14-00_main.md` (предыдущий
   чекпоинт от другой сессии) — не задваивать уже сделанное.
3. `npm install && npm run typecheck && npm run lint && npm run format:check && npm test && npm run build` —
   все пять должны быть зелёными перед любым новым коммитом.
4. Продолжить с пункта P0 из бэклога выше: **вынос localStorage/credentials
   в backend** — самая крупная оставшаяся архитектурная задача.

---
*Следующий чекпоинт: после следующего содержательного шага или перед паузой.*
