# 🚀 ЧЕКПОИНТ: Robot-Buhgalter

## 📌 Мета-информация
- **Дата:** 2026-08-25 13:43 (UTC+3)
- **Обновлён:** 2026-08-25 после первого блока стабилизации
- **Автор:** GPT-5.6 Sol + Михаил
- **Ветка:** `main`
- **Этап:** стабилизация MVP перед дальнейшими интеграциями и SaaS

---

## 🗺️ Где мы находимся

### Текущий этап
- [x] Базовая архитектура User → Organization → TaxRegistration → BusinessUnit → SalesChannel
- [x] ПСН + УСН 6% для двух ИП
- [x] Налоговый календарь 2026 и журнал фактических платежей
- [x] Multi-account marketplace connections, включая два Ozon Client-Id одного ИП
- [x] Контроль полноты данных
- [x] Первый блок исправлений по техническому аудиту
- [ ] Подтвердить зелёный CI после исправлений
- [ ] Автотесты налогового ядра
- [ ] Безопасное серверное хранение данных и credentials
- [ ] Production sync backend
- [ ] Коммерческий SaaS

### ✅ Что исправлено после аудита
1. ESLint больше не валит CI на сгенерированном `src/components/ui/**`; приложение продолжает линтиться.
2. Исправлен regex дат в `src/lib/importer.ts`.
3. `server.pid` удалён; `*.pid` добавлен в `.gitignore`.
4. Удалён устаревший запрос `wb-sync.json` из `Home.tsx`.
5. `useAppState()` теперь возвращает мемоизированный объект со стабильными callback-функциями.
6. Deploy workflow теперь запускает `npm run lint` до сборки: красный lint больше не должен молча уезжать на GitHub Pages.
7. CORS локального sync-сервера ограничен явным списком origin; список можно переопределить через `ALLOWED_ORIGINS`.
8. Обычная резервная копия больше не содержит API-ключи. При восстановлении credentials из старых backup-файлов также отбрасываются.
9. Экспорт для бухгалтерской проверки по-прежнему не включает API secrets.

### ⏳ Что проверяется сейчас
- Новый CI/Deploy запущен после изменений. До результата не считать стабилизацию CI полностью закрытой.

---

## 🔄 В работе прямо сейчас

### P0 — получить зелёный CI
Если CI ещё падает:
1. открыть конкретный job/log;
2. исправить оставшиеся application-level lint/type errors;
3. не ослаблять lint глобально без причины.

### P0 — налоговые regression tests
После зелёного CI:
- подключить Vitest;
- `calcInsurance2026`;
- УСН 6% без работников и с работниками;
- УСН 15% и минимальный налог;
- ПСН и потенциальный доход;
- НДС 2026;
- прогрессивный НДФЛ ИП на ОСНО;
- совмещение ПСН + УСН в `organizationTax.ts`.

### P0/P1 — безопасность и storage
- localStorage остаётся временным MVP-хранилищем, не production;
- API-ключи всё ещё находятся в browser storage в рабочей сессии — до SaaS их нужно перенести в backend secret storage;
- production цель: backend + PostgreSQL + tenant isolation + encrypted secrets;
- публичный сайт + localhost sync остаётся dev-only архитектурой.

---

## 🎯 Ближайшие шаги

| Приоритет | Задача | Статус |
|---|---|---|
| P0 | Восстановить зелёный CI | Проверяется |
| P0 | Удалить server.pid / ignore | ✅ Готово |
| P0 | Удалить wb-sync.json bootstrap | ✅ Готово |
| P0 | Не экспортировать API keys в backup | ✅ Готово |
| P0 | Добавить налоговые unit-тесты | Следом |
| P1 | Добавить Prettier и прогнать форматирование | После CI |
| P1 | Production sync backend вместо localhost | Запланировано |
| P1 | PostgreSQL + auth + tenant isolation + secret storage | Запланировано |
| P1 | Таблица налоговых правил по годам | До поддержки 2027 |

---

## 🧠 Архитектурные решения, которые сохраняем
- Один налогоплательщик может иметь несколько режимов и торговых направлений.
- Два Ozon кабинета ИП Леонтьева — два `SalesChannel`, но одна база УСН 6% этого ИП.
- Банк не заменяет финансовый отчёт маркетплейса при определении налоговой выручки.
- Для розницы ПСН основным источником фискальной выручки будет ОФД/касса; эквайринг используется для сверки.
- Неполные данные не должны отображаться как окончательный налог.
- Платёж из календаря не равен подтверждённой задолженности ФНС без сверки с ЕНС.
- Credentials не должны попадать в экспорт/backup и в коммерческой версии должны храниться только на backend.

---

## ⚠️ Оставшиеся риски
| Риск | Уровень | Следующее действие |
|---|---|---|
| CI ещё не подтверждён зелёным после фиксов | 🔴 Высокий | Проверить текущий Actions run |
| Бухгалтерские данные только в localStorage | 🔴 Высокий | PostgreSQL/backend |
| API secrets в browser storage | 🔴 Высокий | Backend secret storage |
| Нет налоговых regression tests | 🔴 Высокий | Vitest сразу после CI |
| GitHub Pages → localhost sync | 🟠 Важный | Production sync API |
| TAX_2026 захардкожен | 🟠 Важный | Tax rules registry по годам |
| Нет единого formatter | 🟡 Средний | Prettier после CI |

---

## 🔄 Как восстановить работу
1. Открыть этот checkpoint.
2. Проверить последний CI GitHub Actions.
3. Если CI зелёный — сразу перейти к unit-тестам налогового ядра.
4. Затем Prettier/форматирование.
5. После налоговых тестов — production storage/security/backend.

---
*Следующий чекпоинт: после зелёного CI и первого набора налоговых тестов.*
